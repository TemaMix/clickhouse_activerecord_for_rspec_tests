# frozen_string_literal: true

RSpec.describe ClickhouseActiverecordForRspecTests do
  it "has a version number" do
    expect(ClickhouseActiverecordForRspecTests::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
